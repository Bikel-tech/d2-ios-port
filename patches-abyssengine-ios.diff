diff --git a/CMakeLists.txt b/CMakeLists.txt
index 37760a3..3ee9041 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -66,8 +66,22 @@ include_directories(${ZLIB_INCLUDE_DIRS})
 find_package(LibArchive REQUIRED)
 include_directories(${LibArchive_INCLUDE_DIR})
 
-find_package(FFMPEG COMPONENTS AVCODEC AVFORMAT AVUTIL SWSCALE SWRESAMPLE REQUIRED)
-include_directories(${FFMPEG_INCLUDE_DIRS})
+# FFMPEG provides in-game video decoding. On iOS the vcpkg build of ffmpeg
+# (and its libiconv dependency) fails to compile against the very new Xcode/iOS
+# SDKs, so we make it optional and fall back to a stub VideoManager (no video).
+# See README-iOS.md "ffmpeg fallback". When vcpkg does not provide ffmpeg the
+# find_package below silently disables it and we build the stub.
+option(USE_FFMPEG "Build with ffmpeg in-game video support" ON)
+if (USE_FFMPEG)
+    find_package(FFMPEG COMPONENTS AVCODEC AVFORMAT AVUTIL SWSCALE SWRESAMPLE)
+    if (FFMPEG_FOUND)
+        include_directories(${FFMPEG_INCLUDE_DIRS})
+        add_compile_definitions(USE_FFMPEG)
+    else ()
+        message(WARNING "FFMPEG not found; building without in-game video (stub VideoManager)")
+        set(USE_FFMPEG OFF)
+    endif ()
+endif ()
 
 #add_compile_options(-fsanitize=address -g)
 #add_link_options(-fsanitize=address)
diff --git a/src/AbyssEngine.c b/src/AbyssEngine.c
index 3eb2bee..dabd359 100644
--- a/src/AbyssEngine.c
+++ b/src/AbyssEngine.c
@@ -11,14 +11,19 @@
 #include "scenes/SceneIntroVideos.h"
 #include "types/Palette.h"
 #include "util/Crypto.h"
+
+#ifdef USE_FFMPEG
 #include <libavutil/log.h>
+#endif
 
 int main(int argc, char **argv) {
     (void)(argc);
     (void)(argv);
 
     Log_SetLevel(LOG_LEVEL_EVERYTHING);
+#ifdef USE_FFMPEG
     av_log_set_level(AV_LOG_FATAL);
+#endif
     LOG_INFO("Abyss Engine");
 
     LOG_DEBUG("Initializing SDL...");
@@ -34,6 +39,15 @@ int main(int argc, char **argv) {
     memset(config_path, 0, 4096);
 #ifdef _WIN32
     snprintf(config_path, 4096, "%s\\abyss\\abyss.ini", getenv("APPDATA"));
+#elif defined(__APPLE__) && TARGET_OS_IPHONE
+    // iOS: the app is sandboxed, so use SDL's writable preferences path.
+    {
+        char *pref_path = SDL_GetPrefPath("abyssengine", "abyss");
+        if (pref_path != NULL) {
+            snprintf(config_path, 4096, "%sabyss.ini", pref_path);
+            SDL_free(pref_path);
+        }
+    }
 #elif __APPLE__
     snprintf(config_path, 4096, "%s/Library/Application Support/abyss/abyss.ini", getenv("HOME"));
 #else
@@ -48,7 +62,11 @@ int main(int argc, char **argv) {
     sdl_window = SDL_CreateWindow("Abyss Engine", SDL_WINDOWPOS_CENTERED, SDL_WINDOWPOS_CENTERED,
                                   (int)((float)800 * AbyssConfiguration_GetInitialScale()),
                                   (int)((float)600 * AbyssConfiguration_GetInitialScale()),
+#if defined(__APPLE__) && TARGET_OS_IPHONE
+                                  SDL_WINDOW_SHOWN | SDL_WINDOW_RESIZABLE | SDL_WINDOW_ALLOW_HIGHDPI);
+#else
                                   SDL_WINDOW_SHOWN | SDL_WINDOW_RESIZABLE);
+#endif
 
     if (sdl_window == NULL) {
         FATAL(SDL_GetError());
diff --git a/src/audio/AudioStream.c b/src/audio/AudioStream.c
index fb47061..08a098f 100644
--- a/src/audio/AudioStream.c
+++ b/src/audio/AudioStream.c
@@ -1,5 +1,7 @@
 #include "AudioStream.h"
 
+#ifdef USE_FFMPEG
+
 #include "../common/FileManager.h"
 #include "../common/Logging.h"
 #include "../managers/AudioManager.h"
@@ -269,3 +271,49 @@ void AudioStream_Stop(AudioStream *audio_stream) {
     audio_stream->is_playing = false;
     Mutex_Unlock(audio_stream->mutex);
 }
+
+#else /* !USE_FFMPEG — audio decoding disabled (e.g. iOS build without ffmpeg) */
+
+#include "../common/Logging.h"
+
+AudioStream *AudioStream_Create(const char *path) {
+    (void)path;
+    LOG_WARN("AudioStream: ffmpeg unavailable; audio playback disabled (stub)");
+    return NULL;
+}
+
+void AudioStream_Destroy(AudioStream **audio_stream) {
+    if (audio_stream != NULL) {
+        *audio_stream = NULL;
+    }
+}
+
+int16_t AudioStream_GetSample(AudioStream *audio_stream) {
+    (void)audio_stream;
+    return 0;
+}
+
+bool AudioStream_IsLooping(const AudioStream *audio_stream) {
+    (void)audio_stream;
+    return false;
+}
+
+void AudioStream_SetLoop(AudioStream *audio_stream, bool loop) {
+    (void)audio_stream;
+    (void)loop;
+}
+
+void AudioStream_Play(AudioStream *audio_stream) {
+    (void)audio_stream;
+}
+
+bool AudioStream_IsPlaying(const AudioStream *audio_stream) {
+    (void)audio_stream;
+    return false;
+}
+
+void AudioStream_Stop(AudioStream *audio_stream) {
+    (void)audio_stream;
+}
+
+#endif /* USE_FFMPEG */
diff --git a/src/audio/AudioStream.h b/src/audio/AudioStream.h
index 878e80d..7b718fb 100644
--- a/src/audio/AudioStream.h
+++ b/src/audio/AudioStream.h
@@ -4,9 +4,12 @@
 #include "../common/MpqStream.h"
 #include "../common/RingBuffer.h"
 #include "../util/Mutex.h"
+
+#ifdef USE_FFMPEG
 #include <libavcodec/avcodec.h>
 #include <libavformat/avformat.h>
 #include <libswresample/swresample.h>
+#endif
 
 typedef struct AudioStream AudioStream;
 
diff --git a/src/managers/AudioManager.c b/src/managers/AudioManager.c
index 2604712..d980b11 100644
--- a/src/managers/AudioManager.c
+++ b/src/managers/AudioManager.c
@@ -8,8 +8,10 @@ typedef struct AudioManager {
     bool                audio_mute;
     SDL_AudioDeviceID   audio_device_id;
     SDL_AudioSpec       audio_spec;
+#ifdef USE_FFMPEG
     enum AVSampleFormat out_sample_format;
     AVChannelLayout     channel_layout;
+#endif
     struct AudioStream *background_music;
     float               volume[AUDIO_SET_VOLUME_TYPE_MAX];
     float               volume_actual[AUDIO_SET_VOLUME_TYPE_MAX];
@@ -85,6 +87,7 @@ void AudioManager_InitSingleton(void) {
     LOG_DEBUG("Obtained audio spec: %d Hz, %d channels, %d samples", audio_manager->audio_spec.freq,
               audio_manager->audio_spec.channels, audio_manager->audio_spec.samples);
 
+#ifdef USE_FFMPEG
     switch (audio_manager->audio_spec.format) {
     case AUDIO_U8:
         audio_manager->out_sample_format = AV_SAMPLE_FMT_U8;
@@ -101,13 +104,16 @@ void AudioManager_InitSingleton(void) {
     default:
         LOG_FATAL("Invalid audio spec format: 0x%04X", audio_manager->audio_spec.format);
     }
+#endif
 
     AudioManager_SetVolume(AUDIO_SET_VOLUME_TYPE_MASTER, AbyssConfiguration_GetMasterVolume());
     AudioManager_SetVolume(AUDIO_SET_VOLUME_TYPE_MUSIC, AbyssConfiguration_GetMusicVolume());
     AudioManager_SetVolume(AUDIO_SET_VOLUME_TYPE_SFX, AbyssConfiguration_GetSfxVolume());
     AudioManager_SetVolume(AUDIO_SET_VOLUME_TYPE_UI, AbyssConfiguration_GetUiVolume());
 
+#ifdef USE_FFMPEG
     av_channel_layout_default(&audio_manager->channel_layout, audio_manager->audio_spec.channels);
+#endif
     SDL_PauseAudioDevice(audio_manager->audio_device_id, SDL_FALSE);
 }
 
@@ -156,6 +162,8 @@ void AudioManager_SetVolume(enum AudioSetVolumeType audio_set_volume_type, float
 
 SDL_AudioSpec AudioManager_GetAudioSpec(void) { return audio_manager->audio_spec; }
 
+#ifdef USE_FFMPEG
 AVChannelLayout AudioManager_GetChannelLayout(void) { return audio_manager->channel_layout; }
 
 enum AVSampleFormat AudioManager_GetSampleFormat(void) { return audio_manager->out_sample_format; }
+#endif
diff --git a/src/managers/AudioManager.h b/src/managers/AudioManager.h
index a061d52..842ffcd 100644
--- a/src/managers/AudioManager.h
+++ b/src/managers/AudioManager.h
@@ -20,7 +20,14 @@ void                AudioManager_PlayMusic(const char *path, bool loop);
 void                AudioManager_SetVolume(enum AudioSetVolumeType audio_set_volume_type, float volume);
 void                AudioManager_FillBuffer(void *userdata, Uint8 *stream, int len);
 SDL_AudioSpec       AudioManager_GetAudioSpec(void);
+
+/* The channel-layout / sample-format getters are only meaningful when ffmpeg is
+   available (they feed AudioStream/VideoManager resampling). On builds without
+   ffmpeg (e.g. iOS) those callers are stubbed out, so we hide the declarations
+   to avoid pulling in ffmpeg types. */
+#ifdef USE_FFMPEG
 AVChannelLayout     AudioManager_GetChannelLayout(void);
 enum AVSampleFormat AudioManager_GetSampleFormat(void);
+#endif
 
 #endif // ABYSS_AUDIOMAN_H
diff --git a/src/managers/VideoManager.c b/src/managers/VideoManager.c
index 0d7fbbf..4219300 100644
--- a/src/managers/VideoManager.c
+++ b/src/managers/VideoManager.c
@@ -8,14 +8,17 @@
 #include "AudioManager.h"
 #include "InputManager.h"
 #include <assert.h>
+#include <stdlib.h>
+#include <string.h>
+
+#ifdef USE_FFMPEG
+
 #include <libavcodec/avcodec.h>
 #include <libavformat/avformat.h>
 #include <libavutil/opt.h>
 #include <libavutil/time.h>
 #include <libswresample/swresample.h>
 #include <libswscale/swscale.h>
-#include <stdlib.h>
-#include <string.h>
 
 #define AUDIO_RING_BUFFER_SIZE          (1024 * 1024)
 #define DECODE_BUFFER_SIZE              (1024 * 8)
@@ -424,3 +427,53 @@ bool VideoManager__ProcessFrame(void) {
     av_packet_free(&packet);
     return false;
 }
+
+#else /* !USE_FFMPEG — stub build: no in-game video on this platform */
+
+typedef struct VideoManager {
+    Mutex *mutex;
+    bool   is_playing;
+} VideoManager;
+
+VideoManager *video_manager;
+
+void VideoManager_InitializeSingleton(void) {
+    video_manager = malloc(sizeof(VideoManager));
+    memset(video_manager, 0, sizeof(VideoManager));
+    video_manager->mutex = Mutex_Create();
+}
+
+void VideoManager_DestroySingleton(void) {
+    if (video_manager == NULL) {
+        return;
+    }
+    Mutex_Destroy(&video_manager->mutex);
+    free(video_manager);
+    video_manager = NULL;
+}
+
+bool VideoManager_IsPlayingVideo(void) {
+    return false;
+}
+
+void VideoManager_Update(uint64_t delta) {
+    (void)delta;
+}
+
+void VideoManager_Render(void) {
+}
+
+void VideoManager_PlayVideo(const char *path) {
+    /* ffmpeg unavailable on this build (e.g. iOS vcpkg SDK incompatibility):
+       skip in-game video silently so the game keeps running. */
+    (void)path;
+}
+
+int16_t VideoManager_GetAudioSample(void) {
+    return 0;
+}
+
+void VideoManager_StopVideo(void) {
+}
+
+#endif /* USE_FFMPEG */
diff --git a/vcpkg.json b/vcpkg.json
index 6b798cc..3bb76d6 100644
--- a/vcpkg.json
+++ b/vcpkg.json
@@ -7,13 +7,12 @@
   "overrides" : [ ],
   "dependencies" : [ {
     "name" : "libarchive",
-    "version>=" : "3.6.2"
+    "version>=" : "3.6.2",
+    "default-features" : false,
+    "features" : [ "bzip2", "lz4", "lzma", "zstd" ]
   }, {
     "name" : "zlib",
     "version>=" : "1.3"
-  }, {
-    "name" : "ffmpeg",
-    "version>=" : "6.1#2"
   }, {
     "name" : "sdl2",
     "version>=" : "2.30.1"
