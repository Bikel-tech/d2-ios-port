name: Probe macOS runner

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  probe:
    runs-on: macos-latest
    steps:
      - run: echo "macOS runner OK -> $(sw_vers -productVersion 2>/dev/null || uname -a)"
